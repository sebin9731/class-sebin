package com.test.java;

import java.time.LocalDate;
import java.util.Calendar;

public class Ex73_DateTime {
	
	public static void main(String[] args) {
		
		/*
		
			자바 날짜/시간
			1. Date
			2. Calendar
			3. java.time 패키지(JDK 1.8)
				-  LocalDate(날짜)
				-  LocalTime(시간)
				-  LocalDateTime(날짜/시간)
				-  Period(년월일 시간)
				-  Duration(시분초 시간)
		
		
		*/
		
		
		//m1();
		m2();
	}

	private static void m2() {
		
		// 시각 생성
		// 1. 현재 시각 만들기
		LocalDate d1 = LocalDate.now(); // 현재 날짜만
		LacalTime t1 = LocalTime.now(); // 현재 시간만
		
		System.out.println(d1);
		
		// 2. 특정 시각 만들기
		
	}

	private static void m1() {
		
		// 기존 Date, Calendar 결함 > 변경이 가능하다. > 가변(Mutable) 객체입니다.
		Calendar now = Calendar.getInstance();
		
		System.out.printf("%tF\n", now);
		
		now.add(Calendar.YEAR, 10);
		
		System.out.printf("%tF\n", now);
		
		// 년, 일은 1부터 시작하는지? > 년, 일 > 서수(Index)
		// 월이 왜 0부터 시작하는지? > 월 > 서수(X), 열거형(O)
		// 요일이 왜 0부터 시작하는지? > 
		System.out.println(now.get(Calendar.MONTH));
		
	}
	
}

















