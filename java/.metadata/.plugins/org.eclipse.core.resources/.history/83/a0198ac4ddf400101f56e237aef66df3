package com.test.java.obj;

public class Ex72_Record {
	
	public static void main(String[] args) {
		
		/*
		
			Record 자료형
			- 데이터를 저장하기 위한 목적으로 만든 특별한 클래스
			- 불변(Immutable) 객체를 생성하는 것이 목적
			- 생성자, Getter, equals(), HashCode, toString을 컴파일러가 자동 생성
		
		
		*/
		
		// 유저 정보 > 클래스
		
		
		
		//m1();
		m2();
	}//main

	private static void m2() {
		
		// 성적 관리 + Record
		// 1. 학생 Record 설계
		// 2. 집합 > List
		// 3. 성적순으로 정렬 + 출력
		
		
		
		
	}

	private static void m1() {
		
		// 유저 정보 > 클래스
		UserRecord u1 = new UserRecord("hong", "홍길동", 20);
		System.out.println(u1);

		UserRecord u2 = new UserRecord("hong", "홍길동", 20); // 생성자
		System.out.println(u2); // toString()
		
		System.out.println(u2.id()); //Getter
		System.out.println(u2.name());
		System.out.println(u2.age());
		
		UserRecord u3 = new UserRecord("hong", "홍길동", 20);
		
		System.out.println(u2.hashCode()); // 오버라이딩
		System.out.println(u3.hashCode());
		
		System.out.println(u2.equals(u3)); // 동일인
		
		
	}

}


// *** 읽기 전용 클래스
// 1. 생성자 > 초기화
// 2. Getter 구현O, Setter 구현X
class UserClass {
	
	private final String id;
	private final String name;
	private final int age;
	
	public UserClass(String id, String name, int age) {
		this.id = id;
		this.name = name;
		this.age = age;
	}
	
	public String getId() {
		return id;
	}
	public String getName() {
		return name;
	}
	public int getAge() {
		return age;
	}
	
	@Override
	public String toString() {
		return "UerClass [id=" + id + ", name=" + name + ", age=" + age + "]";
	};
};

// *** 읽기 전용 객체
// Record 사용 > 컴파일러가 대신 구현
// - private final 멤버 구현 O
// - 생성자 구현 O
// - Getter 구현 O
// - toString 구현 O
record UserRecord(String id, String name, int age) {
	
};


// 학생 레코드
record StudentRecord(String nam, int kor, int eng, int math) {
	

	// 생성자 유효성 검사?
	// 컴팩트 생성자(Compact Constructor)
	public StrudentRecord {
		
	}
	
}









